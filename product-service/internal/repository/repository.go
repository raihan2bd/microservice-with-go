package repository

// ProductRepository defines the interface for product data operations
type ProductRepository interface {
	// Define methods in the interface as needed, e.g., FindByID, Save, Update, Delete.
}
